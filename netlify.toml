[build]
  base    = "apps/web"
  command = "npm run build"
  publish = "apps/web/dist"

# Required: serve index.html for all routes so client-side paths like
# /board, /command, and /r/:token work on direct open and refresh.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
